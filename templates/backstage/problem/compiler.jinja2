{% extends 'backstage/base.jinja2' %}

{% block content_header %}
  Compiler
{% endblock %}

{% block backstage_content %}
  {% include 'components/message.jinja2' %}
  <p>Please upload the whole project as a zip. C++ is the only language supported temporarily.</p>
  <p>Here is how it works: this tool will find the first *.cpp in your zipfile and compile it. Please do not have your program in a sub-folder.</p>
  <form class="mb-5" enctype="multipart/form-data" method="post" action="{{ request.path }}">
    {% csrf_token %}
    <div class="form-group">
      <input type="file" name="file" class="form-control-file">
    </div>
    <button class="btn btn-primary">Submit</button>
  </form>


{% endblock %}
