<div class="ui list">
  {% for submission in submission_list %}
    <div class="item">{{ submission.create_time | date('Y-m-d H:i:s') }} <h5 class="ui header status-span inline-block" data-status="{{ submission.status }}" data-test="{{ submission.status_test }}" {% if show_percent %}data-sp="1"{% endif %} data-score="{{ submission.status_score }}"></h5> → <a href="{{ url('contest:submission', submission.contest_id, submission.id) }}">{{ submission.id }}</a>, {{ submission.get_lang_display() }}</div>
  {% endfor %}
</div>