<div class="ui list">
  {% for submission in submission_list %}
    <div class="item">{{ submission.create_time | date('Y-m-d H:i:s') }} <h5 class="ui header status-span inline-block" data-status="{{ submission.status }}"></h5>{% if show_percent %} {{ submission.status_percent | round(1) }}{% endif %} → <a href="{{ url('contest:submission', submission.contest_id, submission.id) }}">{{ submission.id }}</a>, {{ submission.get_lang_display() }}</div>
  {% endfor %}
</div>