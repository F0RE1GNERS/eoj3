<div class="ui grid">
  <div class="twelve wide column">
    <form class="ui form" action="{{ url('polygon:session_save_meta', session.id) }}" method="POST" id="save-meta-form">
      {% csrf_token %}
      <input name="next" type="hidden" value="{{ request.path }}">

      <div class="field">
        <div class="two fields">
          <div class="field">
            <label>ID</label>
            <input type="text" readonly name="id" value="{{ problem.id }}">
          </div>
          <div class="field">
            <label>Alias</label>
            <input type="text" name="alias" value="{{ config.alias }}">
          </div>
        </div>

        <div class="two fields">
          <div class="field">
            <label>Time limit (ms)</label>
            <input type="text" name="time_limit" value="{{ config.time_limit }}">
          </div>
          <div class="field">
            <label>Memory limit (MB)</label>
            <input type="text" name="memory_limit" value="{{ config.memory_limit }}">
          </div>
        </div>

        <div class="field">
          <label>Source</label>
          <input type="text" name="source" value="{{ config.source }}">
        </div>

        <div class="four fields">
          <div class="field">
            <label>Description file</label>
            <input type="text" readonly name="description" value="{{ config.description }}">
          </div>
          <div class="field">
            <label>Input file</label>
            <input type="text" readonly name="input" value="{{ config.input }}">
          </div>
          <div class="field">
            <label>Output file</label>
            <input type="text" readonly name="output" value="{{ config.output }}">
          </div>
          <div class="field">
            <label>Hint file</label>
            <input type="text" readonly name="hint" value="{{ config.hint }}">
          </div>
        </div>

        <div class="three fields">
          <div class="field">
            <label>Checker</label>
            <input type="text" readonly name="checker" value="{{ config.checker }}">
          </div>
          <div class="field">
            <label>Interactor</label>
            <input type="text" readonly name="checker" value="{{ config.interactor }}">
          </div>
          <div class="field">
            <label>Validator</label>
            <input type="text" readonly name="checker" value="{{ config.validator }}">
          </div>
        </div>

        <div class="three fields">
          <div class="field">
            <label>Case number</label>
            <input type="number" readonly name="case-number" value="{{ case_count }}">
          </div>
          <div class="field">
            <label>Pretest number</label>
            <input type="number" readonly name="pretest-number" value="{{ pretest_count }}">
          </div>
          <div class="field">
            <label>Sample number</label>
            <input type="number" readonly name="sample-number" value="{{ sample_count }}">
          </div>
        </div>

      </div>

      <button class="positive ui button" type="submit">Save</button>

    </form>
  </div>
  <div class="four wide column">
    <div class="ui info message">
      <div class="header">Quick Reference</div>
      <ul class="list">
        <li>Problem ID is read-only.</li>
        <li>You should be careful to change alias because it may make your teammates confused sometimes.</li>
        <li>Click <b>Save</b> when you are done with this page. Changes might be lost when you come back later.</li>
        <li>There are a lot of read-only fields here. You must edit these fields in other sections. This is just for a quick check.</li>
      </ul>
    </div>
  </div>
</div>
