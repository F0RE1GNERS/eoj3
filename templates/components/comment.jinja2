{% from 'components/username_display.jinja2' import username_display with context %}

{% macro comment_view(comment) %}
  <div class="comment">
    <a href="{{ url('generic', comment.author.pk) }}" class="avatar">
      <img src="{{ comment.author.avatar_small.url }}">
    </a>
    <div class="content">
      <div><a class="author">{{ username_display(comment.author) }}</a> <small class="text-muted"><span class="date">{{ comment.create_time | date('Y-m-d H:i:s') }}</span></small>
        {% if comment.is_privileged %}<a class="text-danger" href="{% if comment.problem %}{{ url('blog:problem_delete_comment', pk, comment.pk) }}{% elif comment.blog %}{{ url('blog:delete_comment', comment.blog_id, comment.pk) }}{% endif %}">delete</a>{% endif %}</div>
      <div class="text">
        {{ comment.text | n2br | safer | safe }}
      </div>
    </div>
  </div>
{% endmacro %}