<a class="user text {% if color %}semi-bold {{ color }}{% endif %} {{ additional_class }}" href="{{ url('generic', user_id) }}">{{ username }} {% if in_contest %}<a class="ui no-margin text {{ color }}" href="{{ url('contest:dashboard', in_contest) }}" data-inverted="" data-tooltip="Contestant"><i class="ui icon flag"></i></a>{% elif legend %}<i class="ui icon cloud"></i>{% endif %}</a>
